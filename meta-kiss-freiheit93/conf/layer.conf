# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-kiss-freiheit93"
BBFILE_PATTERN_meta-kiss-freiheit93 = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-kiss-freiheit93 = "9"

LAYERDEPENDS_meta-kiss-freiheit93 = "core meta-arm meta-kiss"
LAYERSERIES_COMPAT_meta-kiss-freiheit93 = "scarthgap"
