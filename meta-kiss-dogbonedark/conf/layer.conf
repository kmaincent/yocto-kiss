# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-kiss-dogbonedark"
BBFILE_PATTERN_meta-kiss-dogbonedark = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-kiss-dogbonedark = "9"

LAYERDEPENDS_meta-kiss-dogbonedark = "core meta-arm meta-kiss"
LAYERSERIES_COMPAT_meta-kiss-dogbonedark = "scarthgap"
